<%# <%= link_to_edit_space_booking_path(@booking) %>
<div class="container">

`<div class="d-flex flex-row card m-4 p-4 my-booking-card">
  <div>
      <% if booking.space.photo.attached? %>
      <%= cl_image_tag space.photo.key%>
    <% else %>
      <%= image_tag "stock/stock_space.jpg"%>
    <% end %>
  </div>
  <div class="d-flex flex-column">
      <h3><%= booking.space.name %></h3>
      <h4>€<%= booking.space.price.round(0) %> <span id="selectedDates">For selected Dates</span></h4>
      <p>Lorem ipsum dolor sit amet consectetur adipisicing elit. Nemo tempora dolore incidunt enim natus, odio pariatur iusto, id voluptas quae tenetur est recusandae numquam.</p>
      <div class="paymentInfo">
          <p class="mt-3">Payments made: </p>
          <span class="progress-label p-1">€0</span>
          <progress class="yellowProgress" max=<%= booking.space.price.round(2) %> value=<%= rand(100)%>></progress>
          <span class="pull-right p-1">€<%= booking.space.price.round(0) %></span>
      </div>
      <div class="border-top border-bottom dates my-2 justify-content-center">
        <p class="flex-grow-1">Dates:</p>
        <p class="mx-3"><strong><%= booking.booking_start_date %></strong></p>
        <p><strong> <%= booking.booking_end_date %></strong></p>
      </div>
      <div class="d-flex justify-content-end">
        <button class="btn-spaces btn-primary-spaces">Edit Booking</ button>
      </div>

    </div>
`</div>
</div>

<%# <% end %>
