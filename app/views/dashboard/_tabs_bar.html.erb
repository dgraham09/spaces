    <ul class="nav nav-tabs my-3" id="myTab" role="tablist">
      <li class="nav-item" role="presentation">
      <button class="nav-link" id="my-spaces-tab" data-bs-toggle="tab" data-bs-target="#my-spaces" type="button" role="tab" aria-controls="my-spaces" aria-selected="false">My Spaces</butt>
      </li>
    <li class="nav-item" role="presentation">
      <button class="nav-link active" id="my-bookings-tab" data-bs-toggle="tab" data-bs-target="#my-bookings" type="button" role="tab" aria-controls="my-bookings" aria-selected="true">My Bookings</button>
      </li>
    <li class="nav-item" role="presentation">
      <button class="nav-link" id="my-account-tab" data-bs-toggle="tab" data-bs-target="#account" type="button" role="tab" aria-controls="my-account" aria-selected="false">My Account</button>
      </li>
    </ul>

    <div class="tab-content" id="myTabContent">
      <div class="tab-pane fade" id="my-spaces" role="tabpanel" aria-labelledby="my-spaces-tab">
        <ul>
          <% @spaces.each do |space| %>
          <li>
            <%= render "dashboard/my_space_card", space: space, bookings: space.bookings %>
          </li>
        <% end %>
        </ul>
      </div>
      <div class="tab-pane fade show active" id="my-bookings" role="tabpanel" aria-labelledby="my-bookings-tab" >
      <ul>
          <% @bookings.each do |booking| %>
        <li>
          <%= render "dashboard/my_booking_card", booking: booking %>
        </li>
        <%end%>
      </ul>
      </div>
    <div class="tab-pane fade" id="account" role="tabpanel" aria-labelledby="my-account-tab">...
    </div>
    </div>
</div>
